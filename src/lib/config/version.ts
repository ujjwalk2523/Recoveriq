/**
 * Single authoritative application version source.
 * In production or CI, this matches the package.json version.
 */
export const APP_VERSION = '0.1.0';
export const SERVICE_NAME = 'recoveriq';
